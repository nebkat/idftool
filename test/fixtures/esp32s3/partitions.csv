# idftool test partition table — exercises every partition kind idftool touches.
# App slots are kept just big enough for the tiny test app (~160 KB -> 192 KB, 64 KB-aligned) and
# storage small, so the whole flashed region (and thus a dump-bundle) stays under ~1 MB. ESP-IDF
# format (no bootloader/partition table rows; those are implicit in an IDF build).
# Name,     Type, SubType,  Offset,    Size,     Flags
nvs,        data, nvs,      0x9000,    0x6000,
otadata,    data, ota,      0xf000,    0x2000,
phy_init,   data, phy,      0x11000,   0x1000,
factory,    app,  factory,  0x20000,   0x30000,
ota_0,      app,  ota_0,    0x50000,   0x30000,
ota_1,      app,  ota_1,    0x80000,   0x30000,
storage,    data, spiffs,   0xb0000,   0x10000,
